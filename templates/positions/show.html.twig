{% extends 'base.html.twig' %}

{% block title %}Puesto{% endblock %}

{% block body %}
    <h1>Puestos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ position.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ position.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="button-container">
        <button type="button" class="btn btn-dark">
            <a href="{{ path('app_positions_index') }}"> Regresar a la lista </a>
        </button>
        <button type="button" class="btn btn-dark">
           <a href="{{ path('app_positions_edit', {'id': position.id}) }}">Editar</a>
        </button>
    </div>

    {{ include('positions/_delete_form.html.twig') }}
{% endblock %}
