{% extends 'base.html.twig' %}

{% block title %}Edit Positions{% endblock %}

{% block body %}
    <h1>Edit Positions</h1>

    {{ include('positions/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_positions_index') }}">back to list</a>

    {{ include('positions/_delete_form.html.twig') }}
{% endblock %}
