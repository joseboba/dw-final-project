{% extends 'base.html.twig' %}

{% block title %}Edit Positions{% endblock %}

{% block body %}
    <h1>Editar puesto</h1>

    {{ include('positions/_form.html.twig', {'button_label': 'Actualizar'}) }}

    <div class="button-container">
        <button type="button" class="btn btn-dark">
            <a href="{{ path('app_positions_index') }}"> Regresar a la lista </a>
        </button>
    </div>

    {{ include('positions/_delete_form.html.twig') }}
{% endblock %}
