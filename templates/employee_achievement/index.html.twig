{% extends 'base.html.twig' %}

{% block title %}Observaciones de empleado{% endblock %}

{% block body %}
    <h1>Observaciones de {{ employee.fullName }}</h1>

    {% if employee_achievements|length > 0 %}
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Descripción</th>
                    <th>Tipo de observación</th>
                    <th>Fecha</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
            {% for employee_achievement in employee_achievements %}
                <tr>
                    <td>{{ employee_achievement.id }}</td>
                    <td>{{ employee_achievement.description }}</td>
                    <td>{{ employee_achievement.achievementType ? 'Logro' : 'Llamada de atención' }}</td>
                    <td>{{ employee_achievement.achievementDate ? employee_achievement.achievementDate|date('H:m:s d/m/Y') : '' }}</td>
                    <td>
                        {{ include('employee_achievement/_delete_form.html.twig') }}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {% else %}
        <div class="not-found-data">
            <img src="{{ asset('images/activities.png') }}" alt=""No se encontraron registros">
            <p>No hay observaciones registradas.</p>
        </div>
    {% endif %}

    <div class="button-container">
        <button type="button" class="btn btn-dark">
            <a href="{{ path('app_employee_achievement_new', {'employee_id': employee.id}) }}">Ingresar observación</a>
        </button>
         <button type="button" class="btn btn-dark">
           <a href="{{ path('app_employee_index') }}">Regresar</a
        </button>
    </div>
{% endblock %}
