<form method="post" action="{{ path('app_employee_achievement_delete', {'id': employee_achievement.id, 'employeeId': employee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee_achievement.id) }}">
    <button class="btn">Delete</button>
</form>
