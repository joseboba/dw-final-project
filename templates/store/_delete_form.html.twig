<form method="post" action="{{ path('app_store_delete', {'id': store.id}) }}" onsubmit="return confirm('¿Estás seguro de eliminar el registro?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ store.id) }}">
     <button class="btn btn-danger delete-button" style="margin-top: 20px">Eliminar {{ ux_icon('ion:trash-outline') }} </button>
</form>
